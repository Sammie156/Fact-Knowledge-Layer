services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_DB=factdb
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=factstuff
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: